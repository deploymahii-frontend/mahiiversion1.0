version: "3.9"

services:
  frontend:
    build: ./frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: always
