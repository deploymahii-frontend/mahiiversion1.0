export default function DeliveryMap({ currentLocation, destination }) {
    return (
        <section className="flex-1 bg-gray-200">
            <div className="flex h-full items-center justify-center text-sm text-gray-600">
                Live Map
            </div>
        </section>
    );
}
